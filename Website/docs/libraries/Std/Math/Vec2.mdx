---
title: Vec2
---

import { Funcdoc, FuncdocParam, Fielddoc, FielddocStores } from '@site/src/components/docs/docs-utils';

# Std.Math.Vec2

:::under-construction
:::
:::not-implemented
:::

```abs
@public struct Vec2
```

`Vec2` is a structure to manipulate bidimentional vectors.
All coordenates are stored as single precision floats (`f32`)

---

## Fields

<Fielddoc bookmark="x">
    ```abs
    @public let f32 x
    ```
    <FielddocStores type="f32">The X coordenate of the vector.</FielddocStores>
</Fielddoc>
<Fielddoc bookmark="y">
    ```abs
    @public let f32 y
    ```
    <FielddocStores type="f32">The Y coordenate of the vector.</FielddocStores>
</Fielddoc>
